# V1 MLP UX After Architecture Switch

This document describes how Kepos should improve UX after the desktop
architecture moves toward React UI plus a Bare worker backend.

The architecture switch matters because it lets the UI become a product shell
instead of a direct protocol console.

## Goal

V1 MLP should make the first private-home loop feel obvious:

```text
create my home
trust a friend
join a friend's home
chat in the home
read or post to treehole
send a DM
restart and still see durable state where expected
```

The UX should hide protocol machinery until the user needs debug detail.

Kepos should feel like:

```text
a private home for trusted friends
```

not:

```text
a P2P key exchange demo
```

## Product Tone

The UI should be calm, local, and trusted.

Recommended tone:

- quiet rather than loud
- personal rather than corporate
- dense enough for repeated use
- warm enough to feel like a place
- explicit about trust without sounding paranoid

Avoid:

- marketing hero layouts inside the app
- decorative cards inside cards
- platform-social clutter
- raw protocol strings as primary UI
- security theater wording
- meeting-app language for V1

## UX Principle

Every visible surface should answer one of these questions:

1. Where am I?
2. Who is trusted here?
3. What can I do now?
4. What is live and what is durable?
5. What changed because of trust or revoke?

If a UI element does not answer one of these, move it to advanced/debug or
remove it.

## Shared Mental Model

Desktop and mobile should share the same product words:

| Concept            | Product Word    | Avoid As Primary UI        |
| ------------------ | --------------- | -------------------------- |
| profile key        | Profile         | public key                 |
| home room key      | Home            | room key                   |
| signed profile URI | Profile QR      | signed URI                 |
| signed home URI    | Home QR         | address payload            |
| ContactBook entry  | Trusted friend  | contact record             |
| revoke             | Revoke trust    | delete                     |
| treehole           | Treehole        | feed                       |
| room chat          | Home chat       | channel                    |
| DM request         | Message request | encrypted invite bootstrap |

Raw ids and URIs should remain copyable, but they should not define the normal
experience.

Current implementation status:

- The React desktop shell now separates a stable left navigation rail, central
  workspace, and right context panel.
- The desktop right context panel now frames Home and People as product actions:
  invite a friend, join a friend's home, and add trusted friend.
- Home chat, Direct, and Treehole panes now show short two-line empty states before
  content arrives.
- Desktop Home, Direct, and Treehole composers now share one submit button
  component, keeping ids, labels, icons, and disabled state wiring consistent
  across writing surfaces.
- Desktop Treehole like and comment actions now use icon-led shared action
  buttons, so post interactions match the rest of the MLP action language while
  keeping the same like/comment command wiring.
- Desktop Treehole now shows an owner-only disabled composer state when the
  local profile is viewing another owner's signed home.
- Desktop pending and inline direct message requests now offer Ignore alongside
  Accept, matching the mobile non-trusting escape hatch.
- Desktop context actions now use collapsible Home and People task groups, with
  Home open by default and People one click away to reduce first-screen density.
- Desktop context action buttons now share one icon-led action component across
  create, manual join, Home QR share/copy/join, and Profile QR share/copy/trust
  flows, keeping first-run action styling and disabled state wiring consistent.
- Desktop trusted friend rows now show trusted status, source, and trust date
  next to the revoke action so People explains why someone has access.
- Desktop now has first-pass MLP shell polish: visible focus states, hover
  feedback, card depth, accessible current-view rail state, and a narrow-screen
  layout that moves the rail to the top and stacks context actions below the
  active workspace.
- Mobile lobby now uses the same product action words for QR and trust flows:
  My Home QR, Join a home, My Profile QR, Friend profile, and Add trusted friend.
- Mobile lobby now keeps People setup collapsed by default so the first screen
  stays focused on create, join, and scan/trust entry actions.
- Mobile QuickStart now exposes Invite a friend as a first-screen reveal action
  for the user's Home QR, so the create-and-share loop is visible without
  opening People setup.
- Mobile QuickStart now names the first-screen actions as Join a home and Trust
  a friend while keeping QR scanning as the implementation path.
- Mobile QuickStart and People sharing panels now use one shared task header
  shape, so start, invite, and trust actions have the same hierarchy without
  changing their QR or trust command wiring.
- Mobile People management panels now use the same task header shape for
  message requests and trusted friends, making requests, invites, trust, and
  contact management scan as one product surface.
- Mobile Advanced panels for manual Home keys and raw QR details now use the
  same task header shape, keeping debug-only paths visually distinct but
  consistent with the rest of the setup UI.
- Mobile setup actions now use icons consistently on scan, trust, and Advanced
  controls, keeping QR and debug actions easier to scan without changing their
  behavior.
- Mobile setup, Advanced join, QR, trust, and zero-contact Direct entry actions
  now share one icon button component, so primary/secondary, disabled, and icon
  color states stay consistent across the first-run mobile path.
- Mobile Home, Direct, Treehole post, and Treehole comment composers now share
  one send button component, keeping icon-only send actions, disabled styling,
  and accessibility labels consistent across writing surfaces.
- Mobile Direct trusted-contact recipient chips now share one component, keeping
  contact names, selected accessibility state, and active styling aligned across
  the Direct composer.
- Mobile room and Direct Advanced toggles now share one component with normal
  and compact variants, keeping debug entry points accessible and visually
  consistent without promoting raw details.
- Mobile room tabs now use icon-plus-label navigation for Home, Direct,
  Treehole, and People, making the primary surfaces easier to recognize during
  repeated use.
- Desktop rail badges now show pending Direct and People work, and Mobile tab
  badges now show pending Direct and People work, so message requests are
  visible before opening each surface.
- Desktop and mobile navigation accessibility labels include pending counts, so
  badge state is part of the main navigation control instead of only visual
  chrome.
- Desktop People empty panels now use icon-led product empty states for message
  requests and trusted friends, so trust management does not collapse into plain
  placeholder text before any contacts exist.
- Desktop People request and revoke action labels now include the target person,
  so list-level trust decisions remain clear to assistive technology.
- Desktop Home, Direct, and Treehole empty states now render as real icon-led
  list items instead of CSS-generated text, making blank panes visible to both
  users and assistive technology.
- Mobile Home, Direct, and Treehole empty states now share one layout component
  with contextual icons, so blank first-run panes feel intentional instead of
  copied between surfaces.
- Mobile Direct's zero-contact state now uses the same icon-led empty treatment
  before linking users to People, so the trust path reads like product UI rather
  than a plain warning block.
- Desktop Direct's zero-contact contact picker now uses the same icon-led empty
  treatment and links directly to People, so the first Direct message path does
  not fall back to a plain setup warning.
- Desktop Direct trusted-contact buttons now expose selected state and Direct
  recipient labels to assistive technology, matching the mobile recipient
  selection semantics.
- Desktop and mobile trusted-friend metadata now show Home instead of Home room,
  keeping internal room terminology out of normal trust surfaces.
- Mobile room Advanced controls now use the same icon treatment as setup and
  Direct advanced controls, keeping debug entry points visually consistent.
- Mobile People empty management panels now share one icon-led layout, so
  message requests and trusted friends scan as related trust surfaces.
- Mobile QR scanning now has an in-flow permission-denied state with a clear
  cancel action instead of only changing the app notice.
- Mobile lobby now shows profile setup as a loading state and disables
  profile-dependent home, QR, and trust actions until local profile data is
  ready.
- Mobile pending message requests now offer Ignore as a non-trusting escape
  hatch alongside Accept.
- Desktop and mobile pending message request cards now include the request text
  preview, with a short fallback when the request has no message body.
- Pending message request records now keep the request text so previews survive
  persistence and reload.
- Desktop Direct now shows a normal Add trusted friend entry when there are no
  trusted recipients, keeping manual profile id entry as Advanced/debug.
- Mobile trusted friend rows now match desktop by showing trusted status,
  source, and trust date next to the revoke action.
- Mobile direct message request bubbles now use the same Accept/Ignore choice
  and remove ignored requests from the local direct message list.
- Mobile Direct messages now keeps revoke trust out of the send composer;
  People remains the trust management surface.
- Mobile Direct now shows a normal Trust a friend entry when there are no
  trusted recipients, while manual recipient entry stays in Advanced.
- Mobile Treehole now disables the main-post input when the local profile cannot
  post, matching the owner-only hint and disabled send action.
- Mobile Treehole now preserves the last known post permission across backend
  progress updates that do not carry a `canPost` value.
- Mobile People now disables joining another home while already inside a home;
  users must leave first, keeping the active session model explicit.
- Mobile People now keeps empty message request and trusted friend panels
  visible, so the page does not collapse into a blank state.
- Desktop now maps expected Home QR, Profile QR, and trusted-only join failures
  to short product notices while keeping raw errors in Advanced status.
- Mobile room advanced status now keeps raw error detail while normal notices
  stay short.
- Home QR and Profile QR are the normal sharing surface.
- Raw Home/Profile URI output textareas remain present for smoke/debug/copy
  paths, but live inside Advanced details in the React desktop shell.
- Desktop Home and People sharing now expose Copy Home QR and Copy Profile QR
  as normal actions while keeping raw QR details behind Advanced.
- Desktop large QR dialogs are keyboard reachable: they bind to a title, focus
  the close action when opened, close on Escape, and return focus afterward.
- Desktop Direct request, People request, revoke, and large QR close actions now
  use lucide icons with text labels, keeping trust decisions easier to scan
  without turning them into icon-only controls.
- Desktop Direct request and People request actions now share one Accept/Ignore
  action component, keeping icon choice, labels, and aria naming consistent
  across both request surfaces.
- Mobile Direct request and People request actions now use lucide icons with
  text labels for Accept and Ignore, keeping trust decisions consistent with the
  desktop treatment while preserving readable commands.
- Mobile Direct request and People request actions now share one Accept/Ignore
  action component, keeping icon choice, disabled styling, and labels consistent
  across both request surfaces.
- Mobile trusted-friend revoke and Treehole like actions now share one small
  icon-led action component with normal and danger variants, keeping trust and
  post interactions aligned without changing their command wiring.
- Mobile Direct trusted-contact chips now expose selected accessibility state,
  and mobile trusted-friend revoke actions use an icon with their text label.
- Mobile collapsible controls now expose expanded accessibility state for
  Advanced panels, People setup, QR reveals, and Direct manual-recipient details.
- Desktop and mobile product notices now use polite live-region semantics so
  status changes like join, trust, revoke, and error states are announced
  without interrupting the user.
- Desktop primary panes now share one product header component with short
  guidance copy, so Home, Direct, Treehole, and People explain live/durable/trust
  intent consistently without duplicating pane label markup.
- Desktop right context forms now use shared task panel headers for My home,
  Invite or join, and Trusted friend flows, keeping setup, sharing, joining, and
  trust actions visually distinct without changing the underlying commands.
- Desktop lobby and joining notices now say Home instead of room, keeping room
  terminology limited to internal state and protocol naming.
- Paste URI inputs still exist in the normal desktop flow because desktop V1
  does not yet have a camera scanner.

## Desktop UX Direction

Desktop becomes the primary product workbench for V1 MLP and later V2/V3.

Use:

```text
Electron renderer
React
lucide icons
Bare worker bridge
```

### Layout

Recommended desktop layout:

```text
left rail        main surface                    right context
Home            current chat/treehole/DM        people/status/QR
DM
Treehole
People

bottom rail:
Profile / Advanced / Leave
```

The left rail should be stable. It should not collapse into setup controls.

### Home View

Home is the default surface after joining or creating.

It should show:

- home chat
- composer
- current peer count
- trusted people summary
- primary actions: invite, copy home, leave

It should not show raw home key unless the user opens advanced.

Empty state:

```text
No messages yet
```

Primary action:

```text
Invite a friend
```

### People View

People owns trust.

It should show:

- trusted friends
- pending message requests if any
- trust status
- revoke trust action
- profile QR
- trust profile action

Trusting a friend should be framed as:

```text
Add trusted friend
```

The detail view can show:

- local alias
- short profile id
- trust source
- trusted at
- revoked state

### Treehole View

Treehole should feel like a durable wall for the home owner, not a generic feed.

It should show:

- posts
- comments
- likes
- author display names or local aliases
- clear disabled state when writing is not allowed

Empty state:

```text
No posts yet
```

If the local user cannot create a main post, the composer should not look
broken. It should explain the state with one short line:

```text
Only the owner can post here.
```

Comments and likes can stay available when policy allows.

### Direct View

Direct should not require typing profile ids in normal use.

It should prioritize:

- trusted friend picker
- accepted thread state
- incoming request cards
- send composer

Manual profile id entry belongs in advanced/debug.

Current implementation status:

- Trusted friend buttons are visible above the DM composer.
- Pending message request cards say who wants to start a DM, then show the
  request preview.
- Direct message request bubbles use the same social title as pending request
  cards.
- Direct message notices use product language instead of invite/thread wording.
- Manual recipient profile id entry still exists for debug and smoke paths, but
  lives inside Advanced details.

Message requests should read as social actions:

```text
Neil wants to start a DM.
```

not as protocol actions:

```text
Incoming kepos.message.request.v1
```

### QR And URI UX

QR is a sharing action, not a permanent dashboard block.

Use dialogs or side panels for:

- Show Profile QR
- Show Home QR
- Paste Profile URI
- Paste Home URI
- Copy URI

Keep large QR rendering for screen-to-phone scanning.

### Status UX

P2P state should be visible but secondary.

Recommended statuses:

- Starting
- Looking for peers
- Connected
- Syncing treehole
- Offline
- Error

Advanced status can include:

- short home key
- short profile id
- treehole status
- backend worker status
- raw error detail

### Desktop Icon Language

Use lucide for common actions:

- `Home`
- `MessageCircle`
- `Sprout` or `Trees`
- `Users`
- `UserPlus`
- `UserMinus`
- `QrCode`
- `Copy`
- `ShieldCheck`
- `LogOut`
- `Send`
- `Heart`
- `Settings`
- `AlertCircle`

Buttons should be icon-first when the command is common, with a tooltip or short
label when needed. Do not use icons as decoration.

## Mobile UX Direction

Mobile should stay close to the same mental model, but use a phone-native
structure.

Recommended mobile shape:

```text
top header: current home / status
bottom tabs: Home, Direct, Treehole, People
primary action: contextual floating or fixed button
```

Mobile should not mirror the desktop three-column layout.

### Mobile Home

Mobile Home should prioritize:

- current home chat
- composer
- peer status
- invite/join actions behind a header action or People tab

The first screen should not start as a long setup page after the user already
has a profile.

### Mobile People

People should become the home for:

- scan profile QR
- show my profile QR
- scan home QR
- show my home QR
- trusted contacts
- revoke trust

This reduces setup clutter in the chat surface.

### Mobile QR

Mobile has the camera, so scanning should be first-class.

Use:

- `Scan Profile QR`
- `Scan Home QR`
- `Show My Profile QR`
- `Show My Home QR`

Paste URI should exist, but scanning should be the normal mobile path.

### Mobile DM

Mobile DM should use trusted contact chips or a list before any manual profile
id input.

Incoming requests should be cards with:

- alias or short id
- message preview
- accept
- ignore or revoke later

### Mobile Treehole

Treehole should match desktop semantics:

- owner can post
- trusted peers can comment/like when policy allows
- disabled composer states are explicit
- empty state is short

## Onboarding

First run should reduce to three product choices:

```text
Create My Home
Join a Home
Trust a Friend
```

The app can still create profile/home keys automatically, but the UI should not
teach the user about that first.

On desktop, these actions can live in a welcome surface.

On mobile, these actions can live in a compact setup screen plus People tab.

## Error Handling

Errors should be layered:

1. user-facing short message
2. retry or next action
3. advanced detail

Examples:

```text
Could not join this home.
Check that this friend is trusted on this device.
```

```text
P2P backend is not ready.
Try again after it starts.
```

Raw exception messages should be visible in advanced detail, not as the main
toast copy.

## Live Versus Durable

The UI must make the data model honest:

- Home chat is live and ephemeral.
- Treehole is durable.
- DM is durable and pairwise.
- Revoke blocks future access but does not erase already replicated data.

This does not need long explanatory copy. It can be shown through placement and
short labels:

```text
Live home chat
Durable treehole
Direct messages
Trust revoked
```

## Accessibility And Fit

V1 MLP should not ship a cramped UI.

Rules:

- text must not overlap at desktop or mobile sizes
- buttons need stable dimensions
- icon buttons need accessible labels or tooltips
- long ids must wrap or truncate intentionally
- dialogs must be keyboard reachable on desktop
- mobile QR scanner must have clear cancel and permission-denied states

## Implementation Order

1. Define shared product words and command/event names.
2. Move desktop backend behind the Bare worker bridge.
3. Keep existing desktop behavior working through the bridge.
4. Add desktop React renderer and lucide.
5. Build desktop MLP shell: left rail, main surface, right context.
6. Move QR/trust/home join flows into product actions.
7. Align mobile tabs and People/QR structure with desktop words.
8. Add empty, loading, disabled, and error states on both platforms.
9. Run desktop smoke, contact smoke, Android checks, and cross-device smoke.

## Success Bar

The UX pass is successful when a new trusted friend can use Kepos without
learning protocol terms.

Minimum user loop:

1. Neil opens desktop and creates his home.
2. Ada opens mobile and scans Neil's profile QR.
3. Ada trusts Neil.
4. Ada scans Neil's home QR.
5. Ada joins the home.
6. Both exchange home chat.
7. Ada reads or interacts with Neil's treehole.
8. Ada sends Neil a DM request.
9. Neil accepts.
10. Both restart and the durable state still makes sense.

If this loop needs raw key explanation, the MLP UX is not done.
